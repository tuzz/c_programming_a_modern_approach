int main(void) {
  // It's not a good idea for an identifier to contain more than one adjacent
  // underscores because it makes the variables hard to read/distinguish.
  int current__balance;
  int current___balance;
  return 0;
}
